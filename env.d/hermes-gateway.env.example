# 模板：对应 15-hermes-gateway.conf 的 envFilesOverlay。
# 运行时也可用 supervisord HTTP API 写入持久化：
#   PUT http://127.0.0.1:9001/program/hermes-gateway/env
#   （JSON body 含键值；可选 restart=true 重启 hermes-gateway）
# 合并顺序：environment= → envFiles → envFilesOverlay（最后胜出）。
# 请勿把含真实密钥的文件提交进版本库。
#
# Hermes api_server / 推理后端常用变量（按实际提供商填写其一或多组）
# OPENAI_API_KEY=__REPLACE__
# OPENROUTER_API_KEY=__REPLACE__
# ANTHROPIC_API_KEY=__REPLACE__
#
# 如需改监听（默认见 supervisord 片段 API_SERVER_*）
# API_SERVER_HOST=127.0.0.1
# API_SERVER_PORT=8642
